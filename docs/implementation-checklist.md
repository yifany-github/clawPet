# 实施清单与验收对照

## Codex 执行清单映射

1. Monorepo 创建
- 状态：已完成
- 目录：`apps/` `services/` `packages/` `assets/` `docs/`

2. Bridge 状态采集器（CLI 轮询 + 日志监听）
- 状态：部分完成
- 已完成：CLI 轮询（`status --all --json`）+ 标准化输出
- 待完成：`logs --follow` 实时监听与事件抽取

3. pet-state-engine
- 状态：已完成（MVP）
- 包含：时间衰减、互动加成、状态映射、动画切换

4. macOS 端挂件
- 状态：脚手架完成
- 待完成：透明浮窗、WS 订阅、互动按钮、设置页

5. UI 资源加载器
- 状态：基础目录与示例配置完成
- 待完成：运行时资源加载与容错策略

6. 手机端接口预留
- 状态：已完成
- 方式：共享 `shared-types` + Bridge API 合同

7. 持久化与测试
- 状态：部分完成
- 已完成：状态引擎单测
- 待完成：Bridge 集成测试、存档持久化

## 验收标准对照

- 挂件显示在线/离线与忙碌状态：Bridge 已支持，前端待接。
- 状态变化数秒内同步：Bridge 轮询+WS 已具备。
- 喂食/抚摸互动影响数值：已具备。
- 切图可加载替换占位：资源目录和 anim JSON 已具备。
- 崩溃/离线/断网优雅降级：Bridge 已有 mock fallback，前端降级待补。
